apiVersion: v1
kind: Service
metadata:
  name: spring-svc               # 고정 이름
spec:
  type: LoadBalancer
  selector:
    app: spring-app              # 색 라벨 불필요 (SourceDeploy가 내부에서 관리)
  ports:
    - port: 80
      targetPort: 8080
